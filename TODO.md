# TODO

## Editorial ranking improvements

- Add an LLM ranking pass once an API key is intentionally configured.
- Add machine translation for non-English candidate summaries after the deterministic filter narrows the daily set.
- Add Cloudflare Access or another authentication layer before linking the admin route from any public surface.
- Track final editorial decisions over time and use them to tune deterministic scoring weights.
- Add per-source authority weights after enough editorial review data exists.
